

App.registView("login",new App.View({
  el:"#login",
  model:new App.Model()
}),true);

App.registView("login/success",new App.View({
  el:"#login/success",
  model:new App.Model()
}),false);

      